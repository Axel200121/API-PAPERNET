package api.papaer.net.utils;

public enum StatusInventoryMovementType {
    IN,         // Entrada de productos (por compras, devoluciones, etc.)
    OUT,        // Salida de productos (ventas, roturas, etc.)
    ADJUSTMENT  // Ajustes manuales (por auditorías, correcciones, etc.)
}
